# frozen_string_literal: true

RSpec.describe JumperCable do
  it "has a version number" do
    expect(JumperCable::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end

  spec.license = "MIT"
end
